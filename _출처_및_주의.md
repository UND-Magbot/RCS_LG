# ⚠️ 이 폴더는 LG 프로젝트입니다 — 읽고 시작하세요

> 작성: 2026-08-10 · **최종 갱신: 2026-08-11 (정식 clone 으로 전환 완료)**

## 출처 — 현재 상태

- **위치**: `C:\Users\ASUS\Desktop\RCS_LG\`
- **저장소**: GitHub `UND-Magbot/RCS_Basic_VESA_ver`
- **작업 브랜치**: **`feature/luke-lg`** — 원작자 브랜치 `feature/backend_noah` 의 커밋 `6bb5b5c` 에서 분기
  (원작자 Choigyuhwi 가 `feature/backend_noah` 를 계속 수정하므로 같은 브랜치를 쓰지 않음)

### 2026-08-11 전환 이력
처음에는 **ZIP 다운로드본**(`.git` 없음)으로 받아 `Desktop\RCS_Basic_VESA_ver-LG\` 에 두고 있었습니다.
그 상태로는 되돌리기·푸시가 불가능해서 정식 clone 으로 교체했습니다.

- `git clone -b feature/backend_noah` → `feature/luke-lg` 분기
- 구 ZIP 폴더와 **줄바꿈(CRLF) 차이를 제외하고 내용이 100% 동일**함을 대조 확인
  → **ZIP이 정확히 `6bb5b5c` 기준이었음이 확인됨** (기존에 "미확인"으로 적어둔 항목 해소)
- 확인 후 구 ZIP 폴더는 삭제

> 참고: `core.autocrlf=true` 라 clone 본은 CRLF, ZIP 본은 LF 였습니다. 내용 차이가 아니라 정상 동작입니다.

## 🚫 옆 폴더(VESA)와 합치지 말 것

바탕화면에 비슷한 폴더가 하나 더 있습니다. **같은 AutoXing 로봇을 쓰지만 고객사가 다른 별개 프로젝트**입니다.

| | **이 폴더 (LG)** | `RCS_Basic_VESA_ver-feature-backend_noah` (VESA) |
|---|---|---|
| 고객사 | **LG** | **VESA(삼성웰스토리)** |
| git 브랜치 | `feature/backend_noah` (ZIP) | **`feature/luke`** |
| 로봇 | **longjack** (펌웨어 2.12.28-pi64) | crawler_s300_op5 (2.12.21-opi64) |
| 랙 | S300/S600 + **LG / LG2** | S300 / S600 |
| 로컬 DB | **`rcs_lg_db`** | `rcs_vesa_db` |
| 망 | **LTE(M2M)** — 유동 IP·지연 보정 | 로컬 Wi-Fi |
| 태블릿 | **콘솔용 / 로봇부착용 2종** | 1종 |
| 작업일지 | "RCS LG 작업일지" | "RCS Basic VESA 작업일지" |

**코드를 그대로 옮기면 깨집니다.** 로봇 모델·랙 치수·DB 이름·네트워크 전제가 전부 다릅니다.
설계 방식이 참고할 만하면 **읽고 나서 상대 프로젝트에 맞게 새로 작성**하세요.

> 실제 사례(2026-08-10): VESA 쪽에서 호출 대기열을 만들 때, 이 프로젝트가 대기열을
> DB(`dispatch_reservations`)에 저장하는 방식이 더 낫다고 판단해 **그 방식만** 차용했습니다.
> 코드는 한 줄도 복사하지 않고 VESA 구조에 맞게 새로 작성했습니다.

## 이 폴더에만 있는 것 (VESA 쪽에 없음)

- `BackEnd/app/constants/rack_specs.py` 의 **LG / LG2 랙 스펙** (2026-07 실측)
- 프론트 POI 편집의 랙 사이즈 선택지 `LG (0.70×0.50m)` / `LG2 (0.665×0.60m)`
- LTE 지연 대응(맵 재선택 3회 재시도), 부팅 후 자동 복구, 작업 강제 종료 기능
- DB 기반 호출 대기열(`dispatch_reservations`)

## VESA 쪽에만 있는 것 (이 폴더에 없음)

- `tests/` 모의 테스트 4종, `미해결이슈.md`, `현장세팅_체크리스트.md`
- `failure_guide.py` (태블릿 실패 안내 문구 단일 소스)
- 배정 락(`_assign_lock`, 로봇/POI 이중배정 차단)
- 렉 반납 `to_unload_point`, `calculation_failed` 조기 중단

## 커밋/푸시

정식 clone 이므로 **이 폴더에서 바로 커밋·푸시할 수 있습니다.**

```bash
git status                 # 브랜치가 feature/luke-lg 인지 확인
git add -A && git commit -m "..."
git push -u origin feature/luke-lg
```

⚠️ **`feature/backend_noah` 에 직접 푸시하지 말 것** — 원작자가 계속 수정 중입니다.

### .gitignore 에 등록된 것 (커밋되면 안 되는 것)
- `_db/`, `*.sql` — DB 덤프(운영 데이터)
- `BackEnd/static/maps/` — 맵핑 산출물(38MB, DB엔 경로만 저장)
- `BackEnd/venv/`, `frontend/node_modules/`, `frontend/.env.local`

## 로컬 실행 (2026-08-11 기준)

```powershell
# 백엔드 — DB rcs_lg_db(127.0.0.1), 0.0.0.0:8002
cd C:\Users\ASUS\Desktop\RCS_LG\BackEnd
.\run_local.ps1

# 프론트 — http://localhost:3000
cd C:\Users\ASUS\Desktop\RCS_LG\frontend
npm run dev
```

- ⚠️ `BackEnd/requirements.txt` 는 **쓰지 말 것** (인코딩 문제 + 개발 PC 전체 목록 3GB). 실제 필요한 건 9개뿐 —
  `fastapi uvicorn[standard] SQLAlchemy PyMySQL python-jose[cryptography] passlib bcrypt==4.0.1 requests websocket-client apscheduler`
- `bcrypt` 는 **4.0.1 고정** (4.1+ 는 passlib 1.7.4 와 충돌)
- DB 원본 덤프: `_db/undrcs_원본_260811.sql` (DROP+CREATE 포함 → 그대로 import 하면 초기화)

## 상세 문서

- `_테스트계획.md` — 2주 계획 + **진행 현황** + 알려진 미수정 버그 A~F
- `작업일지.md` — 일자별 작업 기록(최신순)
- `.claude/agents/` — 영역별 전담 에이전트 4종(backend / robot / frontend / tablet)
