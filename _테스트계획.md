# LG 프로젝트 테스트·디버깅 계획

> 작성: 2026-08-10 (착수 예정일 2026-08-11)
> 목표: **약 2주간 사내 테스트·디버깅 → 2026-08월 하순 LG 현장 설치**
> 이 기간 VESA(`feature/luke`) 작업은 보류. LG가 주 작업.

---

## 0. 기본 규칙

| 항목 | 결정 |
|---|---|
| **작업 브랜치** | **`feature/luke-lg`** (신규) — `feature/backend_noah` 에서 분기. 원작자와 같은 브랜치를 쓰지 않아 충돌 위험 없음 |
| 실기 | **LG 로봇(longjack) 사용 가능** — 실기 검증까지 진행 |
| 일지 | 이 폴더의 `작업일지.md` 에 "Noah 일일 업무 보고" 양식으로 기록 |
| 검증 표기 | VESA 문서와 동일하게 **[측정]**(직접 확인) / **[추정]**(해석) 구분. 🟢실기확인 / 🔵코드완료-검증대기 / 🔴미해결 |

---

## 1. Day 0 — 환경 구축 (내일 오전, 반나절 예상)

### 1-1. git 연결 ⚠️ 가장 먼저
현재 이 폴더는 **ZIP 다운로드본이라 `.git` 이 없습니다.** 이 상태로 2주 작업하면 되돌릴 수도, 합칠 수도 없습니다.

```bash
git clone -b feature/backend_noah https://github.com/UND-Magbot/RCS_Basic_VESA_ver.git RCS_LG
cd RCS_LG
git checkout -b feature/luke-lg
```

- [ ] clone 받은 코드와 **현재 ZIP 폴더의 차이 확인** (차이가 있으면 ZIP이 오래된 것)
- [ ] 이 폴더(`_출처_및_주의.md`, `_테스트계획.md`)의 문서는 새 폴더로 옮기기
- [ ] 기존 ZIP 폴더는 백업으로 남기거나 정리

### 1-2. 실행 환경
현재 **가상환경·node_modules·.env 전부 없음.**

- [ ] Python 가상환경 생성 + `pip install -r BackEnd/requirements.txt`
      (VESA 때 `python-jose`·`APScheduler` 누락으로 기동 실패한 전례 있음 → 에러 나면 그 두 개부터 확인)
- [ ] `frontend` 에서 `npm install`
- [ ] `frontend/.env.local` 생성 — `NEXT_PUBLIC_API_URL=http://localhost:8002`

### 1-3. DB
- [ ] **`rcs_lg_db` 생성** — 현재 로컬에 없음 (있는 건 `rcs_vesa_db` 21테이블, 빈 `test`)
      작업일지 08-03 기록에 원격 `rcs_vesa_db` → 로컬 `rcs_lg_db` 복제 절차 있음 (21테이블 + FK 26개)
- [ ] **`run_local.ps1` 비밀번호 정정** — 스크립트는 `unde5466` 인데 실제 로컬 DB는 `1234` 로 접속됨 (**불일치**)
- [ ] 포트 확인: LG도 **8002** 사용 → VESA 백엔드와 동시 기동 불가 (2주간 LG만 쓰므로 무방)

### 1-4. 기동 확인
- [ ] 백엔드 `run_local.ps1` → `/ping`, `/health`
- [ ] 프론트 `npm run dev` → 로그인 → 관제 진입
- [ ] **콘솔 화면**(`/api/dispatch/console`) 로드 확인 ← LG의 핵심 UI

---

## 2. ⚠️ 이미 알고 있는 문제 — 재현이 예상되는 것들

**2026-08-10 VESA 작업에서 발견·수정한 버그가 LG 코드에는 그대로 남아 있습니다(코드 확인 완료).**
테스트 중 같은 증상이 나오면 원인을 찾느라 시간 쓰지 말고 아래를 먼저 보세요.

| # | 증상 | LG 코드 상태 | VESA에서의 원인·해법 |
|---|---|---|---|
| **A** | 맵 저장하면 태블릿/콘솔이 "위치 정보를 찾을 수 없어요", 충전소 설정이 사라짐 | **동일 존재** (`crud/map.py` 에 POI delete+재생성 **2곳**) | 저장 시 POI id 전체 변경 + FK `ON DELETE SET NULL` 로 `robots.charging_id` **자동 소실**. 저장 후엔 충전소·POI 주소 재설정 필수 |
| **B** | 맵핑 시작했는데 "맵 데이터 수신 대기 중"에서 안 넘어감 | **가드 없음** (`is_charging` 0건) | **로봇이 충전독에 도킹된 채로는 맵이 안 생김.** 충전기에서 분리 후 재시작 |
| **C** | 위 B와 함께, 맵이 영영 안 그려짐 | **동일 버그** (`/map` 도착만 하면 폴백 잠금) | 빈 `/map` 토픽이 `/maps/5cm/1hz` 폴백을 영구 차단. 실데이터일 때만 잠가야 함 |
| **D** | [작업 종료] 시 랙을 제자리가 아닌 **앞쪽에** 내려놓음 | **동일** (반납 이동이 `standard`) | `standard` 는 목표 근처에서 도착 처리 → VESA 실측 **0.717m** 오차. `to_unload_point` 로 바꿔야 정밀 |
| **E** | 랙 들고 출발 못 하고 멈춤, 화면에 안내 없음 | **동일** (`safe_move` 상한 200회 = 약 17분) | `calculation_failed(9)` 무한 재시도. 공간 부족이 원인이면 절대 안 풀림 |
| **F** | 동기화했는데 `create_time` 이 옛날 날짜 | — | **정상입니다.** 덮어쓰기라 안 바뀜 → `last_modified_time` 으로 판정할 것 |

> ⚠️ **단, LG는 로봇(longjack)·랙(LG/LG2)·펌웨어(2.12.28)가 달라 수치는 다를 수 있습니다.**
> 위는 "같은 코드 경로가 있다"는 사실이지, LG에서 같은 증상이 난다는 확인은 아닙니다.

**대응 방침**: 테스트 중 실제로 재현되면, VESA에 적용한 수정을 **LG 구조에 맞게 이식**합니다.
(코드 복사가 아니라 방식 이식 — VESA 쪽 커밋 `f2231fb` 참고)

---

## 3. 테스트 단계 (권장 순서)

### STEP 1. 로봇 연결 — 여기가 막히면 다 막힘
- [ ] 로봇 IP 확인, `:8090` 접속
- [ ] **LTE(M2M) / WiFi 중 어느 망으로 테스트할지 결정**
      LTE면 **유동 IP 문제**(하루에도 여러 번 변경)를 먼저 처리 — 고정 IP 또는 DDNS
- [ ] 온라인 판정이 `/chassis/current-map` 기준으로 도는지 (LTE에서 `/device/info` 무응답 대응)
- [ ] 대용량(맵핑)은 **WiFi 권장** — 작업일지 08-04 결론

### STEP 2. 맵 / POI
- [ ] 맵핑 (⚠️ 위 B·C 주의 — **충전기 분리 후 시작**)
- [ ] POI 등록: 충전소 `C1`, 경유지 **`C1-1`**(도킹 실패 예방), 랙 위치(standby), 작업 위치
- [ ] **랙 위치 POI 에 `rack_size` 지정** (`LG` / `LG2`) ← LG 고유. 이거 없으면 랙 정렬 실패
- [ ] 동기화 → **`last_modified_time`** 으로 반영 확인
- [ ] 동기화 후 로봇 위치가 유지되는지 (포즈 저장·복원 기능)
- [ ] ⚠️ **저장 후 `charging_id`·`standby_id`·POI 주소 재확인** (위 A)

### STEP 3. 랙 인식 (`align_with_rack`) — LG의 최대 난관
작업일지 08-06에 원리가 정리돼 있음. `Wrong rack size` 가 나오면:
- [ ] 로그의 **`detected(a,b)` 값을 그대로** `rack_specs.py` 에 반영 (도면 치수 아님)
- [ ] `leg_size` 가 실제 다리 지름과 맞는지 (LG2 = 25mm → 0.025). 크면 **캐스터를 다리로 오인**
- [ ] 랙 중앙 **체인/늘어진 물체 제거** (라이다에 가짜 depth 생성 — 소프트웨어로 못 거름)
- [ ] 검출이 흔들리면 관측 범위 **중앙값** 사용 (LG2 depth 0.52~0.57 → 0.545)
- [ ] 캐스터가 장애물로 잡히면 `margin` 확대 (0.05 → 0.08)

### STEP 4. 배차 콘솔 (핵심 운영 UI)
- [ ] 로봇 호출 — **리프팅(렉 포함)** / **리프팅(렉 없이)** / **서빙** 3종
- [ ] 가용 로봇 0대일 때 **예약** 등록 / 예약 취소
- [ ] **경유지 여러 개 등록 + [출발]**
- [ ] 우측 로봇 사이드바 — 온라인·배터리·상태 표시
- [ ] [직접 제어] 모달 — 방향 조종, 잭 업/다운, 충전소·대기장소 복귀, 시스템 재시작
- [ ] **[작업 강제 종료]** — 로봇은 그대로 두고 세션만 정리되는지

### STEP 5. 로봇 부착 태블릿
- [ ] 경유지 도착 시 [확인] 버튼 노출
- [ ] [확인] → 다음 경유지 진행
- [ ] **마지막 경유지 [확인] → 자동 종료(복귀)**
- [ ] [작업 종료] 버튼 동작
- [ ] 콘솔에서 [확인] 대행도 되는지

### STEP 6. 복귀 시퀀스
- [ ] 랙 반납 위치 정확도 (⚠️ 위 D — **앞에 내려놓는지 반드시 눈으로 확인**)
- [ ] `C1-1` 경유 후 충전소 도킹
- [ ] `chassis/moves` 에 **`creator: rcs` + `type: charge`** 가 생기는지로 판정

### STEP 7. 부팅 자동 복구 (LG 고유)
- [ ] 로봇 전원 껐다 켜기 → 온라인 전환 시 **current-map 자동 설정 + 충전소 기준 위치 재조정**
- [ ] 위치 재조정 시 **헤딩이 180° 뒤집히지 않는지** (08-07 수정 이력 있음)
- [ ] 작업 중인 로봇은 제외되는지

### STEP 8. 이상 상황 / 동시성
- [ ] 로봇 2대 이상 동시 운영
- [ ] 같은 POI 동시 호출
- [ ] 이동 중 통신 끊김 → 복구
- [ ] 세션 꼬임 시 [작업 강제 종료]로 회복되는지

---

## 4. LG 고유 리스크 (현장 설치 전 반드시 해결)

| 리스크 | 내용 | 조치 |
|---|---|---|
| 🔴 **LTE 유동 IP** | 로봇 라우터 WAN IP가 재부팅마다 변경(하루에 10.230 → 10.49 → 10.25). 등록 IP로는 접속 불가 | **고정 IP 확보 또는 DDNS 설정** — 실운영 전 필수 |
| 🔴 **뒷다리 검출 편차** | LG2 랙 뒷다리 캐스터 간섭으로 depth ±수 cm. 펌웨어 허용오차 5cm 미만이라 아슬아슬 | 중앙값으로 완화 중. 근본은 **캐스터 물리 정리** |
| 🟡 체인 간섭 | 랙 중앙에 늘어진 체인이 라이다 평면 가로지름 | **물리적 제거** (소프트웨어 불가) |
| 🟡 AutoXing 문의 | `rack_area_id` 사용 불가, `detectRackSize` REST 없음 | 현장 전에 문의해두면 좋음 |

---

## 5. 2주 일정(안)

| 기간 | 내용 |
|---|---|
| **1일차** | 환경 구축(1장) + 기동 확인 + 콘솔 화면 열기 |
| **2~4일차** | STEP 1~3 (로봇 연결 → 맵/POI → **랙 인식**). 랙 인식이 가장 오래 걸릴 가능성 |
| **5~7일차** | STEP 4~6 (콘솔 → 로봇 태블릿 → 복귀). 위 A~E 재현 시 디버깅 |
| **2주차 전반** | STEP 7~8 (부팅 복구, 동시성) + 발견 문제 수정 |
| **2주차 후반** | 전체 사이클 반복 검증, **현장 세팅 체크리스트 작성**(VESA의 `현장세팅_체크리스트.md` 처럼) |
| **현장 설치** | 체크리스트 보고 진행 — **현장은 인터넷이 안 될 수 있음을 전제로 문서화** |

---

## 6. 기록 방법

- 매일 이 폴더 `작업일지.md` 최신순 추가 (Noah 양식)
- 문제 발견 시 `미해결이슈.md` 신설해 VESA와 같은 형식으로 관리 (🔴/🔵/🟢 + [측정]/[추정])
- 현장 설치 전 `현장세팅_체크리스트.md` 작성 — **인터넷 없는 현장에서 이것만 보고 진행 가능하도록**
- 커밋은 기능 단위로. 되돌릴 수 있게 자주.
