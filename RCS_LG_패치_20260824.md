# RCS LG 패치 — 2026-08-24

> **R→J 단방향 배송 모드** 신규 + 현장 배치에 맞춘 UI 재구성(호출=R / 확인=J) + 결함 6건 수정.
> 개발 PC(192.168.30.2)에서 **실기 5사이클 무중단 순환 검증 완료** 후 서버 노트북 이관용.
> 백엔드 **8파일**. 프론트엔드 변경 **없음**. 태블릿 APK 는 **`poi` 1종만 신규 설치** 필요.

---

## 0. 한 줄 요약

| # | 항목 | 성격 | 실기 검증 |
|---|---|---|---|
| 1 | **R→J 단방향 배송 모드** (`job_points` 매핑) | 신규 기능 | ✅ 5사이클 |
| 2 | **호출 버튼을 J → R 로 이동**, J 는 [확인]만 | 현장 요구 반영 | ✅ |
| 3 | 예약이 통째로 사라지고 충전소로 복귀 | 결함 (`picking_up` 오판) | ✅ |
| 4 | 강제 종료를 "랙 없음"으로 오판 → **중지가 소비됨** | 결함 (안전) | ✅ |
| 5 | 랙 없음으로 끝나면 세션이 안 닫혀 R 타일이 계속 "작업 중" | 결함 (유령 세션) | ✅ |
| 6 | 화면이 20초씩 멈췄다 튐 / 버튼이 되돌아감 | 결함 (폴링 3종) | ✅ 40회 측정 |
| 7 | `rack-cleared` 가 R id 를 받으면 조용히 무동작 | 결함 | ✅ |

---

## 1. 적용 방법

### 1-1. 백엔드 정지

작업 중인 로봇이 없는지 먼저 확인할 것. **랙을 들고 이동 중에 서버를 내리면 로봇이 그대로 멈춘다.**

```
관제 콘솔에서 모든 로봇이 유휴(작업 중 아님)인지 확인
→ 백엔드 콘솔 창에서 Ctrl+C
```

### 1-2. 파일 덮어쓰기

zip 안의 `BackEnd/` 를 서버의 `C:\RCS_LG\BackEnd\` 에 **경로 그대로** 덮어쓴다.

```
BackEnd/app/models/__init__.py
BackEnd/app/models/dispatch.py
BackEnd/app/crud/dispatch.py
BackEnd/app/schemas/dispatch.py
BackEnd/app/routers/dispatch.py
BackEnd/app/services/dispatch_service.py
BackEnd/app/templates/dispatch_console.html
BackEnd/app/templates/dispatch_tablet.html
```

> ⚠️ **`default_area.json` 은 넣지 않았다.** 개발 PC 값(`area_id: 26`)이라 덮어쓰면
> 현장 맵이 엉뚱하게 바뀐다. 현장 값을 그대로 둘 것.

> ⚠️ DB 스키마는 **손댈 필요 없다.** `job_points` 테이블은 서버 시작 시
> `Base.metadata.create_all` 이 **없으면 자동 생성**한다. 수동 `CREATE`/`ALTER` 없음.

### 1-3. 백엔드 시작

```
cd C:\RCS_LG\BackEnd
.\run_local.ps1
```

시작 로그에 오류가 없으면 정상.

---

## 2. ★ 적용 후 반드시 해야 하는 설정 3가지

이걸 안 하면 **동작은 하는데 배송 모드로 안 들어가거나, 복귀만 조용히 안 된다.**

### 2-1. 로봇에 충전소·대기장소 지정

**`register-by-ip` 로 등록한 로봇은 `charging_id`/`standby_id` 가 비어 있다.**
비어 있으면 작업은 전부 되는데 **마지막 복귀만 조용히 안 된다.**

```
관제 → 로봇 관리 → 해당 로봇 → 충전소 = C1 / 대기장소 = R1 선택
```

- 충전소 = **필수**
- 대기장소 = 보험(부팅 복구 폴백). R1/R2 아무거나
- **백엔드 재시작 불필요**
- 반드시 **화면에서 이름으로** 고를 것 — 현장 POI id 는 개발 PC 와 완전히 다르다

확인 로그 (안 되면 이게 찍힌다):
```
[delivery] 충전소 POI 없음 — robot=NN
[relocalize] ...: 충전소/대기장소 POI 미지정 — 위치재조정 생략
```

### 2-2. 작업지점↔랙보관 매핑 등록

배송 모드는 **매핑이 있는 POI 에서만** 동작한다. 매핑이 없으면 종전 인터랙티브 모드로 돈다.

먼저 현장 `area_id` 와 POI 이름을 확인한 뒤:

```
POST http://127.0.0.1:8002/api/dispatch/job-points
  {"area_id": <현장 area>, "j_poi_name": "J1", "r_poi_name": "R1"}
  {"area_id": <현장 area>, "j_poi_name": "J2", "r_poi_name": "R2"}
```

확인:
```
GET http://127.0.0.1:8002/api/dispatch/job-points
```

> **맵을 새로 만들면 area_id 와 POI id 가 바뀐다.** 맵 작업 후 매핑을 다시 등록할 것.

### 2-3. 태블릿 4대 주소 설정

각 태블릿에서 **서버 주소 + POI ID** 두 칸만 넣는다.

```
서버 주소 : http://<서버 주소>:8002
POI ID    : R1 / R2 / J1 / J2 각각의 실제 POI id
```

POI id 확인:
```
GET http://127.0.0.1:8002/api/dispatch/console/status
  → pois[].poi_id / poi_name
```

브라우저로도 열린다 — `http://<서버>:8002/api/dispatch/tablet/poi/{poi_id}`
(크롬 → 홈 화면에 추가 하면 전체화면 앱처럼 동작. **APK 없이도 테스트 가능**)

---

## 3. 태블릿 APK

**`poi` flavor 만 새로 설치하면 된다.** 콘솔·로봇 앱은 **재설치 불필요** —
화면은 서버가 주는 HTML 이라 앱을 껐다 켜면 이번 UI 변경이 그대로 반영된다.

| 파일 | 패키지 | 앱 이름 | 조치 |
|---|---|---|---|
| `APK/app-poi-release.apk` | `com.und.rcs.tablet.poi` | RCS 위치태블릿 | ★ **신규 설치** (현장 4대) |
| (기존) 관리자콘솔 | `...tablet.console` | RCS 관리자콘솔 | 그대로 사용 |
| (기존) 로봇태블릿 | `...tablet.robot` | RCS 로봇태블릿 | 그대로 사용 |

패키지 id 가 달라 **한 기기에 셋 다 설치 가능**하다.

```
adb install -r app-poi-release.apk
```

---

## 4. 바뀐 동작 — 현장에서 뭐가 달라 보이나

### 4-1. 호출은 R, 확인은 J

현장은 랙 보관 위치(R)와 작업지점(J)이 **100m 이상 떨어져 서로 다른 구역**에 있다.

| 구역 | POI | 그 구역 작업자 |
|---|---|---|
| A | **R1 + J2** | R1 에서 호출 → J1 로 배송 / J2 에서 확인 |
| B | **J1 + R2** | R2 에서 호출 → J2 로 배송 / J1 에서 확인 |

- **J 에는 호출 버튼이 없다.** 랙이 눈앞에 있는 R 옆 사람만 호출한다
- **R 화면이 짝 J 의 랙 상태를 보여준다.** 랙이 남아 있으면 호출 버튼이 **비활성**
- 버튼은 사라지지 않고 **자리에 남아 비활성**으로 표시된다(왜 못 누르는지 문구로 안내)

### 4-2. 랙 점유 해제는 사람만 할 수 있다

랙 감지 센서가 없고, **라이다 판정도 불가능하다**(로봇이 자기가 든 랙을 인식해 버림 — 실측).
**J 태블릿의 [확인 — 랙을 치웠습니다] 가 유일한 해제 수단**이다.
누르지 않으면 짝 R 의 호출 버튼이 살아나지 않는다.

### 4-3. 큐가 있으면 충전소를 안 간다

하차 후 대기 예약이 있으면 충전소를 건너뛰고 그 예약의 R 로 바로 간다.
검증에서 **5사이클 연속 무중단 순환**을 확인했다.

### 4-4. 숨긴 기능 (배송 모드에서만)

경유지 등록 · 다음 위치 지정 · 서빙 호출 · 렉 없이 호출 — 이 공정에 없어서 감췄다.
**삭제가 아니라 숨김**이라, 매핑 없는 POI 에서는 종전 화면이 그대로 뜬다.

---

## 5. 고친 결함 상세

### 5-1. 예약이 통째로 사라지고 충전소 복귀

`occupied_poi_ids()` 의 "떠나는 중" 판정에 **`picking_up` 이 빠져 있었다.**

배송 워커는 J 하차 후 다음 작업을 이어받을 때 `current=이전 J` 를 남긴 채
`status=picking_up` + `target=다음 R` 로 간다. 그런데 판정이 `moving` 만 인정해서
**이미 떠난 J 가 계속 점유로 잡혔고**, 그 J 를 호출/예약하면 409 로 거부됐다.
→ 예약이 안 만들어져 로봇이 그냥 충전소로 갔다.

```python
leaving_current = (st == "returning") or (st in ("moving", "picking_up") and t is not None)
```

기존 인터랙티브 모드는 `picking_up` 일 때 `current` 가 비어 있어 드러나지 않았다.

### 5-2. 강제 종료를 "랙 없음"으로 오판 ⚠️ 안전

`_pickup_at_poi` 가 `align_with_retry` 의 예외를 `except Exception` 으로 잡아 `False` 를 돌려줬다.
`jack_service` 에서 `RuntimeError` 를 던지는 곳은 **중지 신호 두 곳뿐**인데 그것까지 삼켰다.

- 활동 로그에 **거짓 `rack_missing`** 이 남는다
- **`_check_stop` 이 플래그를 `pop` 하므로 중지가 소비된다** → 대기 예약이 있었다면
  **강제 종료를 눌렀는데 로봇이 다음 작업을 시작**한다

→ `except RuntimeError: raise` 로 그대로 올려보낸다(세션 `failed` + 예약 롤백).

### 5-3. 랙 없음으로 끝나면 세션이 안 닫힘 (유령 세션)

```python
if nxt is None:
    break        # 주석은 "충전소 복귀" 였지만 실제로는 아무 일도 안 함
```

세션이 `picking_up` 인 채 남고, 그 세션의 `target(R)` 때문에
**로봇이 충전소에 있는데 R 타일이 계속 "작업 중"** 으로 보였다.
→ `returning` → 충전소 도킹 → `completed` 로 실제 복귀시킨다.

### 5-4. 화면이 20초씩 멈췄다 튐 / 버튼 되돌아감

| 원인 | 조치 |
|---|---|
| `console/status` 안에서 **오프라인 로봇을 실제로 찔러** 타임아웃 대기(15초마다 반복) | 화면 폴링은 `online_ips_cached(block=False)` — 캐시로 즉답, 갱신은 백그라운드. 오프라인 캐시 TTL 60초. **실제 배차는 종전대로 정확히 확인** |
| 대기 예약을 가용 로봇에서 안 뺌 → 로봇이 바쁜데 "가용 1대" | 예약 수만큼 차감 |
| 낡은 폴링 응답이 낙관적 화면을 덮어씀 | `refreshSeq` 로 **응답 도착 후에도** 최신인지 검사 + 그리드 DOM diff |

**40회 연속 측정** — 이전 최대 20초+ 타임아웃 → **최대 0.033초, 1초 초과 0회**

> 현장은 로봇이 오프라인일 때가 많아(설치 전/전원 오프) **이 수정이 특히 체감된다.**

### 5-5. `rack-cleared` 가 R id 를 받으면 무동작

`set_job_point_occupied` 가 `j_poi_name` 으로만 조회해서, R 이름을 넘기면 매칭이 빗나가
**`{"ok":true}` 로 성공 응답하면서 점유는 그대로 남았다.**
→ R id 로 불러도 짝 J 를 풀도록 치환 추가.

---

## 6. 롤백

**코드를 되돌릴 필요 없다. 매핑만 지우면 종전 동작으로 돌아간다.**

```
DELETE http://127.0.0.1:8002/api/dispatch/job-points/J1?area_id=<현장 area>
DELETE http://127.0.0.1:8002/api/dispatch/job-points/J2?area_id=<현장 area>
```

매핑이 없으면 배송 모드로 진입하지 않고, UI 도 종전 화면(`legacyTileHtml` / 기존 태블릿 화면)이
그대로 뜬다. 롤백 경로를 보존하려고 옛 코드를 지우지 않고 남겨뒀다.

테이블까지 지우려면 `DROP TABLE job_points` (기존 테이블과 무관하다).

---

## 7. 적용 후 확인 체크리스트

| # | 확인 | 방법 | 기대 |
|---|---|---|---|
| 1 | 백엔드 기동 | `GET /ping` | 200 |
| 2 | 매핑 등록됨 | `GET /api/dispatch/job-points` | J1→R1, J2→R2 |
| 3 | 콘솔에 R 타일이 보임 | 콘솔 화면 | R1·R2·J1·J2 4개 (R→J 짝으로 정렬) |
| 4 | 폴링이 안 멈춤 | 콘솔을 1분 지켜보기 | 20초씩 멈추는 구간 없음 |
| 5 | 로봇 충전소 지정됨 | 로봇 관리 화면 | 충전소·대기장소 채워짐 |
| 6 | 배송 모드 진입 | R1 호출 후 백엔드 로그 | `[dispatch] 배송 모드 — robot=NN R1 → J1` |
| 7 | 하차 후 점유 표시 | J1 타일 | [확인 — 랙을 치웠습니다] 활성, R1 호출 비활성 |
| 8 | 확인 후 재활성 | J1 [확인] | 2초 뒤 R1 호출 버튼 살아남 |
| 9 | 충전소 복귀 | 큐 비우고 대기 | C1-1 경유 후 도킹, 세션 `completed` |

**6번이 안 찍히면** 매핑을 못 찾아 기존 모드로 빠진 것이다 — `area_id` 를 확인할 것.

---

## 8. 남은 제약 (이번 패치에 없음)

| # | 내용 |
|---|---|
| 1 | **divert 미구현** — 충전소로 향하는 이동이 시작된 뒤 들어온 호출은 그 구간이 끝나야 전환된다. 확인 지점이 이동 시작 전 2회뿐 |
| 2 | **상태 문구가 화면에 안 뜬다** — 통신이 끊기면 `safe_move` 가 5초 간격으로 **최대 17분 재시도**한다(자동 복구는 장점). 그동안 화면엔 "이동 중"만 보여 작업자가 이유를 모른다 |
| 3 | **서버 재시작 복구 미검증** — 작업 중 백엔드 재기동 시 동작을 아직 확인하지 못했다. 현장 정전·재부팅 대비 필요 |
| 4 | 하차 정밀도 — `standard` 이동이라 목표 근처에서 도착 처리(VESA 실측 0.717m). `to_unload_point` 전환 검토 미적용 |

---

상세 사양·근거는 [docs/08_공정시나리오_변경.md](docs/08_공정시나리오_변경.md) 참조.
