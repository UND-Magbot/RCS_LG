# RCS LG 작업일지

> 매일 작업한 내용을 최신순(위쪽이 최신)으로 추가.

---

Noah 일일 업무 보고 <2026.08.11>

1. RCS LG 개발 (개발환경 구축 · 실DB 이관 · 로봇 연결 · 배차 실동작 확인)

    저장소·개발환경 구축
     - ZIP 다운로드본으로 작업하던 것을 정식 clone 으로 교체하고 작업 브랜치를 분리(원작자 브랜치와 충돌 방지). 구 ZIP 폴더는 내용이 동일함을 대조 확인한 뒤 정리
     - Python 가상환경 구성. 기존 requirements 파일이 인코딩 문제로 읽히지 않는 데다 개발 PC 전체 패키지 목록(딥러닝·비전 등 3GB 상당)이라 사용 불가하여, 백엔드가 실제로 사용하는 9개만 선별 설치. 이 과정에서 스케줄러 패키지가 두 requirements 파일 어디에도 없다는 점을 발견해 별도 설치
     - 실행 스크립트의 DB 비밀번호가 로컬과 불일치하고 가상환경 경로가 지정돼 있지 않아 실행 자체가 안 되던 문제 수정
     - 프론트 의존성 설치 및 API 주소 설정. 백엔드·프론트 기동 후 관제 화면과 콘솔 화면이 정상 표시되는 것까지 확인

    운영 DB 이관
     - 그동안 임시로 쓰던 타 프로젝트 복제본 DB를 실제 LG 운영 DB로 교체(21테이블·FK 26개·한글 정상). MySQL 덤프를 MariaDB로 복원하며 방언 차이 없이 통과함을 확인
     - 이관 후 등록 로봇이 longjack 1대이고 랙 위치 POI에 LG/LG2 사이즈가 지정된 상태임을 확인. 원본 덤프는 로컬 보관하되 저장소에는 올라가지 않도록 처리

    로봇 연결
     - 로봇이 LTE 라우터 망에 있어 서버 PC와 서로 다른 망이었고, 해당 망은 인터넷이 되지 않는 문제를 확인. 맵핑 등 대용량 작업의 안정성을 고려해 로봇을 사무실 WiFi로 이전하기로 결정
     - 로봇 WiFi 설정 진입 경로가 기존 기종과 달라 신규 확인(로봇 관리자 화면 → 시스템 설정 → 구 네트워크 설정 열기 → 이후는 기존과 동일)
     - 이전 후 로봇의 실제 IP를 대역 스캔으로 특정하고 DB 갱신. 관제에서 온라인 인식과 속도 설정 전달까지 정상 동작 확인
     - 온라인 판정이 무거운 정보 조회가 아니라 가벼운 현재맵 조회 기준으로 되어 있어 LTE 환경에서도 안전하다는 점을 코드로 재확인

    맵/POI 재구성 및 배차 실동작
     - 기존 맵으로 진행하려 했으나 경로 문제로 맵을 새로 작성. 신규 영역·맵과 POI 7개(충전소, 충전소 경유지, 랙 위치, 작업 위치 4개)를 구성하고 로봇의 충전소·대기장소 설정을 신규 POI로 재지정
     - 로봇 호출 → 랙 픽업 → 작업 위치 이동 → 경유지 이동까지 실제로 동작하는 것을 확인(확인 대기 상태까지 도달)

    랙 위치에서 로봇이 출발하지 못하는 문제 규명
     - 증상: 랙을 든 채 작업 위치로 출발하지 못하고 경로 계산 실패가 반복됨. 화면에는 안내가 없어 원인 파악이 어려웠음
     - 진단: 로봇의 이동 이력에서 실패 사유가 "경로 계산 실패(도로 미연결)"임을 확인. 위치추정·잭 상태·랙 적재는 모두 정상이었고 목표 지점도 통행 가능 영역이었음
     - 원인 규명: 로봇이 보관 중인 지도 이미지를 직접 받아 분석한 결과, 랙 위치 주변 반경 0.4m 안에 장애물로 기록된 점 4개가 있었고 그 배치 간격이 LG 랙의 다리 배치(폭 0.70m, 깊이 0.50m)와 정확히 일치. 맵핑 당시 그 자리에 있던 랙의 다리가 지도에 영구 장애물로 기록된 것으로 확인
     - 검증: 로봇 반폭만큼 여유를 준 연결성 계산에서 출발점 자체가 막힘으로 판정(목표 지점들은 모두 도달 가능). 나머지 POI 6곳은 동일 검사에서 이상 없어 랙 위치 한 곳만의 문제로 특정
     - 조치: 랙을 치운 상태로 맵을 새로 작성해 해결. 향후 맵핑 시 랙·대차를 모두 치우고 진행하기로 정리

    전용 에이전트 4종 정리
     - 프로젝트 규모를 실측해(백엔드 약 1.4만 줄, 프론트 약 1.9만 줄, 태블릿 앱 181줄, 배포 65줄) 영역별 전담 문서를 작성
     - 백엔드(배차 상태머신·DB·배포), 로봇(제어·랙 인식·맵 동기화·네트워크), 프론트(관제 화면), 태블릿(콘솔/로봇 태블릿 UI) 4종으로 구성. 태블릿 앱과 배포는 규모가 작아 각각 태블릿·백엔드 문서에 통합

2. 배차 반복·동시 조작 검증 및 동시성 결함 2건 수정

    검증 환경 구축 (실기·운영 DB와 완전 분리)
     - 콘솔·태블릿을 여러 번 반복해서 조작할 때 DB나 백엔드가 꼬이는 구간이 있는지 확인하기 위해 모의 테스트 환경을 구성
     - 운영 DB를 복제한 테스트 전용 DB를 만들고, 그 안의 로봇 IP를 도달 불가 주소로 치환. 로봇과 통신하는 함수는 전부 가짜로 대체하고 실제 통신이 한 번이라도 시도되면 테스트가 실패하도록 계측 → 전 구간에서 실기 통신 0회 확인
     - 타 프로젝트의 기존 동시성 테스트는 DB까지 전부 메모리로 대체해 배정 로직만 검증하는 구조였으나, LG는 경유지 순회·태블릿 확인 흐름이 관심사여서 DB 정합성 자체를 봐야 하므로 실제 DB를 그대로 사용하는 방식으로 새로 작성
     - 9개 시나리오 구성: 정상 사이클 반복, 출발 동시 연타, 확인 연타, 같은 위치 동시 호출, 종료 중 명령, 강제 종료 반복, 예약 대기열, 재시작 복구, 점유 해제

    1차 결과 판별 (테스트 자체의 결함 분리)
     - 1차 실행에서 9건 실패로 나왔으나, 정상 사이클이 첫 회부터 실패하는 것은 실기에서 이미 정상 동작을 확인한 것과 모순되므로 테스트 쪽을 먼저 의심
     - 원인: 확인 명령을 보낸 직후 서버가 상태를 바꾸기까지 짧은 지연이 있는데, 그 사이에 테스트가 같은 지점의 확인을 두 번 소비해 다음 지점에서 멈춘 것. 확인 횟수가 아니라 경유지 순번 기준으로 판정하도록 수정
     - 수정 후 재실행: 반복 안정성 관련 항목이 모두 통과 → 반복 조작으로 DB·백엔드가 꼬이는 구간은 없음을 확인. 남은 실패는 동시 조작 2건으로 좁혀짐

    결함 A — 콘솔 [출발]이 동시에 여러 건 수락됨
     - 증상: 출발 요청이 겹치면 2~3건이 모두 성공 처리됨. 경유지는 화면(태블릿)이 읽는 DB와 로봇이 실제로 사용하는 값 두 곳에 기록되는데, 요청이 겹치면 이 둘이 서로 달라져 태블릿에 표시되는 경유지와 로봇이 실제 가는 곳이 어긋남(1차 15회 중 3회 재현, 타이밍이 맞을 때만 발생)
     - 발생 조건: 콘솔 창을 두 개 띄우거나, 응답이 느려 경로를 바꿔 다시 누르는 경우(LTE 환경에서 현실적)
     - 조치: 상태 검사부터 DB 반영·워커 전달까지를 로봇별 잠금으로 묶어 한 덩어리로 처리. 먼저 들어온 요청이 잠금 안에서 상태를 이동중으로 바꾸므로 뒤따라온 요청은 거부됨

    결함 B — 같은 위치에 여러 대가 배정됨
     - 증상: 같은 작업 위치를 동시에 호출하면 로봇 3대가 모두 그 자리로 배정됨
     - 원인: 로봇 쪽은 배정 슬롯을 원자적으로 선점하는 방어가 있으나 위치 쪽에는 같은 방어가 없어, 점유 검증과 실제 배정 사이의 틈으로 여러 요청이 통과
     - 조치: 위치 단위 선점 장치를 추가. 타 프로젝트는 호출 처리 전체를 하나의 잠금으로 감싸는 방식이나, 그 안에 로봇 온라인 실시간 조회(캐시 만료 시 수 초 소요)가 포함돼 있어 그대로 이식하면 과거에 해결한 화면 지연 문제가 재발할 수 있다고 판단. 잠금 안에서는 메모리 집합만 다루고 네트워크 조회는 밖에 두는 방식으로 구현
     - 참고: 로봇이 1대인 동안에는 기존 로봇 슬롯 방어로 이미 막히고 있어 동작 변화가 없음. 로봇 증설 시 의미가 생기는 수정

    수정 후 검증
     - 9개 시나리오 19개 항목 전부 통과. 정상 사이클·예약 대기열 등 기존 통과 항목이 그대로 유지되어 회귀 없음을 확인
     - 원인 미상이던 관찰(같은 로봇에 세션이 두 번 생기는 현상)도 함께 사라짐 → 별도 결함이 아니라 위치 경쟁으로 배정 재시도가 반복되며 생긴 부산물로 판명
     - 부수 효과: 전체 테스트 시간 305초 → 179초. 경쟁하던 요청이 즉시 거부되며 헛도는 재시도가 사라진 결과로, 실제 운영에서도 동시 호출 시 응답이 빨라짐

3. 자동 사이클 스크립트 준비 (다음 작업용)

    목적과 방식
     - 호출 → 경유지 등록 → 각 도착마다 확인 → 복귀를 사람이 버튼을 누르지 않고 반복 실행해 무인 안정성을 보기 위한 것
     - 백엔드·프론트는 수정하지 않고, 콘솔이 사용하는 API를 그대로 호출하는 외부 스크립트로 구성(검증을 마친 백엔드 코드를 그대로 유지하기 위함). 도착 후 5초 대기 뒤 자동 확인
     - 확인 버튼은 원래 사람이 안전을 승인하는 절차이므로 이 스크립트는 테스트 전용이며 현장 운영에는 사용하지 않는 것으로 정리

    이상 감지 시 정지 절차
     - 이동 취소로 로봇을 그 자리에 세운 뒤(랙은 든 채, 잭은 내리지 않음) 세션을 정리하고 정지 시점 상태를 파일로 저장. 사용자 중단(Ctrl+C)도 같은 절차를 따름
     - 감시 항목: 경로 계산 실패 연속 발생, 비상정지 버튼, 원격 모드 전환, 바퀴 과부하, 잭 상태로 인한 이동 거부, 세션 실패, 단계별 시간 초과
     - 로봇 응답 형식을 실기로 검증하는 과정에서 두 가지를 확인·수정: 잭·위치추정·배터리는 조회 방식이 달라 별도 경로가 필요했고, 이동 이력 조회는 개수 제한 파라미터가 무시되어 전체 이력이 반환됨
     - 후자로 인해 어제 발생했던 실패 기록을 현재 상황으로 오인해 즉시 정지하는 오탐이 발생 → 시작 시점의 최신 이동 번호를 기준선으로 잡아 그 이후만 판정하고, 정상 완료가 나오면 실패 누적을 초기화하도록 수정. 재검증에서 정상 판정 확인

4. 남은 정리 사항
     - 구 맵·영역이 비활성 처리되지 않아 POI 목록에 신구가 섞여 보임. 다만 콘솔 노출 목록은 현재 적용 영역 기준으로만 조회되어 실제 혼선은 발생하지 않음을 확인
     - 대표 층 설정이 두 영역에 중복 지정되어 있으나, 해당 값을 사용하는 로직이 없어 기능 영향은 없음을 코드 확인
     - 자동 사이클 스크립트는 호출 직전까지만 사전 검증된 상태로, 비상 정지가 실제로 로봇을 세우는지와 각 단계 소요 시간은 실기에서 확인 필요

---

Noah 일일 업무 보고 <2026.08.07>

1. RCS LG 개발 (동기화 위치 보정 · 부팅 자동 복구 · 배차 안정화 · 강제 정리 기능)

    동기화 후 위치 보정 완성 및 맵 설정 안정화
     - current-map 재선택이 LTE·펌웨어 지연으로 한 번에 안 먹어 맵이 설정 안 되던 문제 → 설정 여부를 확인하며 최대 3회 재시도하도록 보강
     - 동기화 시 맵 재선택 전 로봇 포즈를 저장했다가 재선택 후 복원해 위치가 리셋되지 않도록 처리하고, 유휴(충전소 도킹) 로봇은 동기화 후 충전소 기준으로 정확히 위치 재조정

    로봇 부팅 후 자동 복구 추가
     - 로봇 부팅 시간이 30초~1분으로 길어지면서 부팅 직후 맵 미설정·위치 리셋되던 문제 대응
     - 로봇이 오프라인→온라인(부팅 완료)으로 전환되면 자동으로 current-map 설정 + 충전소 기준 위치 재조정을 수행하는 백그라운드 복구 기능 구현(작업 중인 로봇은 도킹 위치가 아닐 수 있어 제외)

    위치재조정 방향(헤딩) 오류 수정
     - 충전소 기준 위치재조정 시 로봇 방향이 180° 뒤집히던 문제 수정 — 충전소 POI 방향을 그대로 로봇 헤딩으로 적용(수동 재조정·부팅 자동 복구 양쪽 반영)

    인터랙티브 배차 안정화
     - 방금 떠난 위치(예: J1)를 곧바로 호출/예약할 때 "점유중"으로 거부되고 화면이 롤백되며 깜빡이던 문제 수정 — 다른 곳으로 이동 중인 로봇의 출발지를 점유 판정에서 제외
     - 실행 중인 배차 작업을 로봇 이동 없이 즉시 정리하는 "작업 강제 종료" 기능을 관제 메인화면·콘솔의 로봇별 원격제어에 추가(로봇은 물리적으로 그대로 두고 세션만 종료 → 로봇 즉시 재사용 가능, 충전소 복귀는 별도 버튼)

2. 달서구청 그림그리기 로봇 개발 (별도 협동로봇 프로젝트)

    앱 종료 시 로봇 전원 처리 점검 및 안전 높이 상향
     - 종료 로직 확인: 로봇 전원 OFF(cobot_power.py servo_off)에는 안전 위치로 이동 후 전원 차단하는 로직이 있으나, 앱의 종료 버튼(SystemOff)은 로봇 전원 처리가 없어 서보 ON 상태로 잔류하던 문제 확인
     - 조치: 전원 차단 전 이동 높이가 낮아 안전 여유가 부족했던 것을 보완 — cobot_power.py(약 97행) 이동 Z값을 400 → 500으로 상향(XY·자세는 유지), dist_draiwin/python 배포 복사본에도 동일 반영

---

Noah 일일 업무 보고 <2026.08.06>

1. RCS LG 개발 (LG 2호 랙 스펙 추가 · 랙 인식 튜닝 · 배차 콘솔 확장 · 배차/동기화 버그 수정)

    LG 대차 2번째 버전(LG2) 랙 스펙·POI 추가
     - 도면(다리중심 665×600, 캐스터 GDS-100) 기준 LG2 랙 스펙을 신규 추가하고, 프론트 POI 편집 화면에 LG2 사이즈 선택지 추가
     - 동기화 시 맵에 등록된 POI 사이즈 종류대로 rack.specs 를 로봇에 전송하도록 구성(LG·LG2 혼재 시 두 스펙 모두 반영)

    랙 인식 실패 디버깅 및 rack.specs 실측 튜닝 — "Wrong rack size" 반복 문제 해결
     - 원리 규명: rack.specs 의 width/depth 는 도면 치수가 아니라 로봇 라이다가 실제로 재는 "다리 중심 간격(detected)" 에 맞춰야 매칭이 통과함을 확인하고 실측값 기준으로 재설정
     - 다리 오인식 수정: 실측 다리 지름 25mm 에 맞춰 leg_size 50mm→25mm, foot_radius 0.0125 로 조정 → 다리 옆 캐스터(약 50mm)를 다리로 잡던 문제 해소
     - 체인 간섭 규명: 랙 중앙에 늘어져 바닥에 끌리는 체인이 라이다 평면을 가로질러 depth 가짜값(0.37·0.47)을 만들던 것을 확인 → 물리적으로 체인 정리 필요(소프트웨어 필터 불가)
     - 뒷다리 검출 편차 대응: 체인 제거 후에도 캐스터 간섭으로 depth 가 0.52~0.57 로 흔들려, 관측 범위 중앙값(0.545)으로 설정해 펌웨어 허용오차 안에 수렴시킴
     - 캐스터 장애물 오인 대응: 튀어나온 캐스터가 진입 시 장애물로 잡히던 문제를 margin 0.08(랙 풋프린트 확장)로 완화

    배차 콘솔 기능 확장
     - 콘솔 우측에 등록 로봇 사이드바 추가 — 로봇별 온라인 여부·배터리·작업 상태 표시, 카드의 [직접 제어]로 관제와 동일한 원격제어(방향 조종·잭 업/다운·충전소/대기장소 복귀·시스템 재시작) 모달 제공
     - 콘솔에서도 로봇 부착 태블릿의 [확인]을 대신 눌러 다음 경유지로 진행/종료할 수 있게 추가

    인터랙티브 배차 버그 2건 수정
     - 출발지가 "경유지 예약"으로 잘못 표시되던 문제 — 이동 중인 로봇의 현재 위치를 예약 표시에서 제외하고, 미래 목적지(다음 경유지)만 예약으로 표시하도록 분리
     - 출발 직후 화면이 롤백(호출 버튼이 깜빡)되던 문제 — 경로 등록이 워커 스레드로 비동기 처리되며 폴링이 이전 상태를 보여주던 것을, 출발 상태를 동기적으로 먼저 반영하도록 수정

    맵 동기화 후 로봇 위치 리셋 문제 해결
     - 원인: 동기화 과정의 current-map 재선택(오버레이 리로드)이 로봇을 리로컬라이즈시켜 현재 위치가 초기화되던 것으로 규명
     - 해결: 재선택 직전 로봇 포즈를 WebSocket(/tracked_pose)으로 저장한 뒤, 재선택 후 POST /chassis/pose 로 복원해 위치가 유지되도록 처리(오버레이·랙 동기화에 적용, 실기 왕복 검증 완료)

---

Noah 일일 업무 보고 <2026.08.04>

1. RCS LG 개발 (LTE 환경 매핑·로봇 통신 디버깅)

    매핑 미동작 원인 추적 — LTE(M2M)로 연결 시 매핑 화면에 맵·로봇 위치가 안 그려지던 문제
     - 증상 정리: 같은 라우터(로컬망)에서는 매핑이 되고, 서로 다른 라우터(LTE M2M)로 연결할 때만 매핑이 안 되던 상태에서 원인 추적
     - 통신 경로 정밀 측정: 로봇 제어·상태·ping 같은 소량 데이터는 되는데 매핑 실시간 데이터(스캔·맵·위치)만 안 오는 것을 확인하고, 처음엔 대량 전송 대역폭 문제 또는 MTU 문제로 의심
     - MTU 배제: M2M 경로에서 최대 크기(1500) 패킷도 손실 없이 통과함을 ping(DF)으로 확인해 MTU 문제가 아님을 확정
     - 로봇 SLAM 상태 점검: 매핑 세션은 생성되나 로봇이 위치를 못 잡는(lidar_matched=false) 상태를 발견, 위치 초기화(global positioning)로 위치 추정이 살아나는 것을 확인

    진짜 원인 규명 — LTE 유동 IP로 인한 로봇 접속 단절
     - 로봇 라우터 WAN IP가 재부팅·네트워크 설정 변경마다 계속 바뀌는 것(하루에 10.230 → 10.49 → 10.25 등 여러 차례) 확인
     - 등록된 옛 IP로는 라우터까지 ping은 되지만(그 IP가 다른 M2M 장비에 재할당됨) 로봇 8090 API에는 연결되지 않아, 매핑·상태·정보 조회가 모두 실패하던 것으로 판명
     - 로봇 내부 IP(192.168.39.100:8090)로 직접 접속해 로봇 API 자체는 정상(200 응답, nginx, 맵 2FF 유지)임을 확인 → 로봇·서비스 문제가 아니라 접속 경로(IP) 문제로 확정
     - 현재 WAN IP로 재설정 후 재검증: device 정보 조회(모델 longjack, 펌웨어 2.12.28), 매핑 스캔(/scan_matched_points2)·로봇 위치(/tracked_pose)·맵 토픽이 모두 정상 수신, 맵 이미지(대용량) 다운로드도 정상 → 그동안의 매핑 미동작·대량 전송 실패·정보 조회 실패가 전부 IP 불일치가 원인이었음을 규명

    부수 확인 및 정리
     - 앞서 세운 가설(device/info 경로가 LTE에서 막힘, 매핑 대역폭 한계, 펌웨어/토픽 불일치)은 모두 IP 불일치가 만든 착시였음을 정정
     - 포트포워딩 규칙(192.168.39.100 / 8090 / 8090 / TCP)은 정상임을 재확인
     - 남은 핵심 과제: LTE 회선이 유동 IP라 접속이 계속 끊기는 문제 → 고정 IP 확보 또는 라우터 DDNS(고정 이름) 설정이 실운영 전 필수임을 정리

---

Noah 일일 업무 보고 <2026.08.03>

1. RCS LG 개발 (LTE 네트워크 전환 세팅 및 로봇 통신 디버깅)

    LTE 전환 네트워크 구성 및 통신 검증 — WiFi 로컬망에서 LTE(M2M) 망으로 로봇 통신 전환
     - 보유 라우터 확인: ESTLA TC700(관리 화면 브랜드 RED Dolphin), LTE CAT.4 / LAN 2포트 / WiFi 2.4GHz. VPN 메뉴는 없고 포트포워딩·DDNS는 지원
     - 회선 성격 파악: 유심 APN이 m2m-router.lguplus.co.kr(LG U+ M2M 전용 APN)임을 확인. WAN IP가 10.x 사설 대역(CGNAT)이라 공인 IP 직접 접속은 불가하지만, 같은 전용 APN 장비끼리는 통신 가능한 구조로 정리
     - 장비 간 통신 실증: 서버 PC 라우터(WAN 10.240.x)에서 로봇 라우터(WAN 10.230.x)로 ping 성공 확인 → 전용 APN 사설망에서 장비 간 통신이 허용됨을 검증(장비 격리 아님)
     - 포트포워딩 설정: 로봇 라우터에 8090 포트를 로봇 내부 IP(192.168.39.100)로 포워딩 → 서버에서 로봇 제어 API(/chassis/*, /services/*)와 WebSocket이 LTE로 정상 접속됨을 확인
     - CGNAT / 전용 APN / 포트포워딩 개념 정리 및 로봇마다 라우터를 두는 구성안, 서버 PC를 같은 APN 망에 연결하는 방법 정리

    백엔드 LTE 대응 수정 — LTE 지연·특정 경로 응답 불가에 맞춘 통신 로직 보정
     - /device/info 응답 불가 발견: LTE 경로에서 /device/info 만 응답이 오지 않음(25초 0바이트, 응답 크기 무관). 이 경로에 의존하던 온라인 판정·맵 연결이 모두 실패하던 문제 해결
     - 온라인 판정 경로 교체: 무거운 /device/info 대신 가벼운 /chassis/current-map 으로 판정하고, 맵이 없어 404 가 와도 '응답이 오면 온라인'으로 처리하도록 수정(로봇관리·맵관리 양쪽)
     - 원격 조종 속도 개선: 방향키(twist) 명령마다 WebSocket 을 새로 열어 LTE 에서 회당 0.5~1.7초 걸리던 것을, 연결을 재사용하도록 변경해 최초 연결 후 전송이 거의 0초로 단축
     - 타임아웃 전반 상향: 로봇 통신 타임아웃을 LTE 지연에 맞춰 상향(2·3초→8초, 5초→10초 등), 이동 폴링 주기·상태 캐시도 조정해 오프라인 오판·명령 유실 방지
     - 라이브 온라인 체크에 IP별 TTL 캐시를 두어 매 요청마다 수 초 걸리던 상태 조회를 완화

    로컬 개발 DB 구성 — 원격 rcs_vesa_db 를 로컬 rcs_lg_db 로 복제
     - 192.168.0.21(MariaDB)의 rcs_vesa_db 를 로컬(MySQL 8) rcs_lg_db 로 복제: 21개 테이블 전체 + FK 26개 + 데이터 이관, MariaDB→MySQL 방언 차이 보정
     - 백엔드 실행용 환경변수 스크립트(run_local.ps1) 정리 — DB 이름 미지정 시 구 DB에 붙어 500 오류가 나던 문제 방지

    로봇 원격제어·맵·매핑 실동작 디버깅
     - 로봇 실제 상태를 WebSocket/REST 로 직접 측정해 어떤 경로가 LTE 로 되고 안 되는지 전수 확인(제어·상태·배터리는 정상, /device/info 만 불가)
     - 매핑 진단 중 로봇 current-map(현재 맵)이 풀린 것을 발견해 2FF 맵으로 복구
     - 매핑 이슈 확인: 매핑 세션은 생성되나(POST 201) 로봇이 실제 매핑 모드로 전환되지 않고, 실시간 스캔·맵·위치 데이터가 오지 않음. lidar_matched=false(로봇 위치 미확보) 및 코드가 기대하는 토픽(/tracked_pose·/trajectory 등)과 로봇 실제 토픽 불일치 확인
     - 원인 분리: 통신(LTE/WebSocket)은 정상이며, 매핑 미동작은 네트워크 속도가 아니라 로봇 SLAM 상태·펌웨어/모델 차이 문제로 판단(로봇 SN 체계가 기존과 다름 — B 계열). 로봇 소프트웨어 업데이트 후 재검증 예정

---

Noah 일일 업무 보고 <2026.06.19>

1. RCS Vesa 개발
    태블릿 — 도착 시 화면 전환 즉시화
     - 도착 후 잭다운 sleep(8초) 동안 state=empty로 잡혀서 다음 명령 화면이 한참 뒤에 뜨던 문제
     - `_set_db_status("awaiting_next")`를 잭다운 전에 호출 → 폴링이 즉시 arrived 인식

    태블릿 — 다음 위치 보낸 후 풀스크린 출발 알림 (2단계)
     - 빈 화면으로 바로 전환되어 사용자가 "보냈다"는 인지 안 됨 → 풀스크린 오버레이
     - 1단계 `⟳ 출발 준비 중` (8초, pulse ripple + border spinner)
     - 2단계 `✓ {POI} 으로 출발` (2초)
     - 백엔드 잭업 끝나는 시점과 동일하게 8초 후 ✓ 전환

    워커 흐름 — 잭업 후 status="moving" 설정
     - 기존: `_set_target_poi` → `status=moving` → `_jack_up_step` (잭업 전에 moving 진입)
     - 변경: `_set_target_poi` → `_jack_up_step` → `status=moving` → `_move_to_poi`
     - status="moving" 시점이 **실제 이동 시작 시점**과 일치 (UI 상태와 정합)

    잭 안정화 시간 조정
     - JACK_SETTLE_SEC: 10초 → 5초 → 잭 미완료 출발 발생 → **8초로 고정**
     - 잭 모터 동작 4-6초 + 안전 buffer 2-4초

    태블릿 spinner 정중앙 정렬 수정
     - ⟳ 글자 회전이 폰트 baseline 차이로 비대칭 → 글자 빼고 **CSS-only border spinner**
     - `top:50%; left:50%; margin:-32px 0 0 -32px`로 박스 정중앙 위치
     - 박스 자체는 ripple pulse, 안의 spinner는 회전 (두 애니메이션 독립)

    자체 confirm 모달 + 종료 버튼 모드별 텍스트
     - 브라우저 alert/confirm → 다크 톤 자체 모달 (`confirmModal` Promise)
     - with_rack 모드별 텍스트 분기: "(렉 반납 + 충전소 복귀)" vs "(충전소 복귀)"

2. Zishen 자율주행 개발
    장비 확인
     - 라이다(Livox Mid-360S)랑 로봇 IP 찾음
    ROS2 설치
     - Humble + Zenoh 깔고 네트워크 고정
    라이다 연결
     - ROS2로 점군 받기 성공
    SLAM 빌드 
     - 네비 스택 20개 패키지 빌드
    매핑 성공
     - 라이다로 지도 만드는 거(FAST-LIO) 작동 확인
---

Noah 일일 업무 보고 <2026.06.18>

1. RCS Vesa 개발

TabletApp Debug APK 빌드 + VESA 아이콘
- 다크 네이비 그라데이션 + 흰색 V + 초록/파란 점 디자인
- Adaptive Icon (Android 8+) + PNG 5종 (Python PIL 생성)

호출 시 렉 유무 선택 + 태블릿 호출 버튼 2개 분리
- `dispatch_sessions.with_rack` 컬럼 추가, body로 받음
- 빈 상태에서 `🟦 렉 포함` / `⬜ 렉 없이` 2개 버튼

잭 동작 with_rack 무관 항상 동작
- 매 도착 시 jack_down(작업 대기), 매 출발 시 jack_up(운반)
- 렉 없이 모드도 잭 사이클 유지 (안전 이동)

가용 로봇 수/선정 ONLINE 체크
- 오프라인 로봇 자동 제외 → 표시 N대 = 실제 호출 가능 N대
- `fetch_all_robots_live` 16-worker 병렬 사용

맵 동기화 시 robot.area_id 자동 갱신 + "층 전환" 버튼 제거
- `api_sync_map_to_robot`에서 타겟 로봇 area_id 자동 업데이트
- 관제의 수동 층 전환 버튼 제거 (불필요)

OperationsDashboard 신규 (관제 우측 패널)
- 4섹션: 작업 위치 / 로봇 상태(한줄 카드) / 오늘 KPI / 최근 이벤트
- 데이터 소스 4개 병렬 폴링 (dispatch/status, robots/live, active-pois, sessions/today)

로봇별 고유 색상 8종 팔레트
- robot_id로 자동 매핑 — 작업 위치/충전소 어디서든 동일 색
- 점유 시 진한 색 + 두꺼운 테두리 + 그림자, 비어있음 회색 점선

KPI 데이터 소스 변경 + 패널 이름 변경
- `task_history` → `dispatch_sessions/today` (VESA 흐름은 task_history 미사용)
- 신규 API `GET /api/dispatch/sessions/today`, 우측 패널명 "운영 현황"

자체 confirm 모달 + 종료 버튼 모드별 텍스트
- 브라우저 alert/confirm → 다크 톤 자체 모달 (`confirmModal` Promise)
- with_rack 모드별: "(랙 반납 + 충전소 복귀)" vs "(충전소 복귀)"

태블릿 깜빡임 fix
- 도착 직후 잭다운 10초 동안 state=empty로 잡히던 문제
- `_poi_status` arrived 조건 확장 (moving + target=NULL도 포함)

태블릿 레이아웃 fix
- POI 그리드 stretching → `grid-auto-rows: 110px` + `align-content: start`
- 강제 제어 버튼 좌측 → 우측 하단, 종료 버튼과 겹침 해결

버그: 렉 없이 호출 시 첫 POI 이동 누락
- with_rack 분기 들여쓰기 실수로 첫 POI 이동까지 픽업 안에 들어가 있던 것
- 수정: 첫 POI 이동 블록을 분기 밖으로

rcs_basic_db → rcs_vesa_db 재복제
- vesa에 어제 추가한 맵/POI 데이터 없어서 전체 재동기화
- 19 테이블 LIKE+INSERT + FK 23개 복원

---

Noah 일일 업무 보고 <2026.06.04>

1. RCS Vesa 개발

RCS-Basic 메인 코드 전체 동기화
- 메인(`RCS-Basic`)의 발전된 코드를 VESA로 흡수: `jack_service.py` +70줄, `scheduler.py` +32줄, `robot.py` +17줄, 신규 `routers/settings.py`, `autoxing_api/`, `robot_api/`, `scripts/`
- VESA 고유는 보존: `dispatch_*` 6개 파일 (모델/스키마/CRUD/서비스/라우터/HTML)
- VESA 패치 재적용: `main.py`(dispatch 라우터 + recover_on_startup 훅), `models/__init__.py`(DispatchSession/TabletSlot import), `constants/robot_types.py`(interactive_relay work_mode), `SideNav.tsx`(작업관리 메뉴 제거), `MonitoringClient.tsx`(DispatchTabs 제거), `MainActivity.kt`(슬롯 URL)
- 운영 데이터 보존: `default_area.json`, `static/maps`, `frontend/.env.local`
- 검증: 백엔드 import OK, jack_service 시그니처 호환(`_get_standby_poi`, `safe_move`, `jack_up`, `jack_down`, `update_job_status`, `_running_robot_id_by_ip` 등 8개), robot.py 강제 제어 API 4개(`jack`, `cancel-move`, `dock`, `return-to-standby`) 호환

DB 전환 (rcs_basic_db → rcs_vesa_db)
- `.env`의 `DB_NAME`을 `rcs_vesa_db`로 재설정 (전체 동기화로 RCS-Basic 기본값 `rcs_basic_db`로 덮였던 것 복구)
- `rcs_basic_db`의 `tablet_slots`(슬롯 1: poi_id=734 "작업1") + `dispatch_sessions` 데이터를 `rcs_vesa_db`로 이전
- Docker 백엔드 재시작 필요 (`docker-compose up -d --force-recreate backend`) — 컨테이너는 시작 시점 환경변수 캡처

VESA 충전소 도킹 — 2단계 흐름으로 변경 (RCS-Basic과 동일화)
- 기존: `_return_to_standby_and_park`가 jack_down 후 곧장 `charge` 호출 (1단계, 사전 접근 없음)
- 변경: standby → jack_down → **사전 접근(`<charger_name>-1` POI standard 이동, best-effort)** → **charge 도킹** (2단계)
- 신규 헬퍼 `_find_charge_approach_poi(area_id, charger_name)` — 같은 area 활성 맵에서 `{name}-1` POI 검색, 없으면 충전소 좌표 그대로 사전 접근에 사용
- 1단계는 실패해도 charge 단계로 진행 (RCS-Basic 정책과 동일)

---

Noah 일일 업무 보고 <2026.05.29>

1. RCS Vesa 개발

VESA 태블릿 — 슬롯 시스템 도입
- 태블릿이 외우는 ID를 POI ID(410, 411 …) → **슬롯 번호(1, 2, 3 …)** 로 단순화
- `tablet_slots` 테이블 신규 (slot_number PK, poi_id FK, alias optional)
- API: `GET/PUT/DELETE /api/dispatch/slots/{n}`, `GET /api/dispatch/slots`, `GET /api/dispatch/slots/available-pois`
- URL: `GET /api/dispatch/tablet/{slot}` — 매핑 없으면 페이지 내에서 **설정 모드** 자동 표시 (POI 드롭다운 + 별명 + 저장)
- 헤더 우상단 ⚙ 버튼으로 언제든 슬롯 매핑 재설정 가능
- 기존 `GET /api/dispatch/tablet/poi/{id}` 는 호환용으로 유지
- TabletApp: 설정 다이얼로그 "POI ID" → "슬롯 번호" 로 변경, prefs 키 `poi_id` → `slot_number`

라우팅/UX 후속 수정
- 레거시 `/tablet/{robot_id}` 라우터 제거 (슬롯 라우터와 경로 충돌로 "레거시 모드" 페이지가 떴던 문제 해결)
- `/slots/available-pois`를 `/slots/{slot_number}`보다 먼저 정의 (FastAPI 라우팅 우선순위 충돌 해결 — "available-pois"가 int로 파싱되던 422 에러 수정)
- **슬롯 설정 POI 드롭다운**: 맵관리 "메인 적용"된 area의 활성 맵 POI만 표시 (`_current_area_id` 활용). 맵 적용 변경 시 자동 반영

---

Noah 일일 업무 보고 <2026.05.28>

1. RCS Vesa 개발

GitHub 저장소 분리
- 기존 `UND_RCS_Basic` origin을 `RCS_Basic_VESA_ver`로 교체하고 `feature/backend_noah` 브랜치 push (커밋 `33bc638`, 658 파일 변경)
- `.gitignore`에 `업무보고_*.md` 패턴 추가

VESA 운영 방식 설계 (인터랙티브 배차)
- 로봇 3대 동시 운영, 작업 포지션 3~10개
- 잭은 시작 시 1회 업, 종료 시 1회 다운 (포지션마다 잭 사이클 없음)
- 첫 작업 포지션만 사전 지정, 다음 위치는 매번 태블릿에서 지시
- 종료 명령 시 standby로 복귀 → 잭다운 → 충전소 도킹

VESA 모드 백엔드 구현
- `models/dispatch.py` — `dispatch_sessions` 테이블 (상태 머신 영속)
- `schemas/dispatch.py`, `crud/dispatch.py` — Pydantic + CRUD
- `services/dispatch_service.py` — 로봇당 워커 스레드 1개, `threading.Event` 기반 next/end
- `routers/dispatch.py` — API 5개 (`start`, `next`, `end`, `status`, `tablet`)
- `templates/dispatch_tablet.html` — POI 그리드 (점유 비활성), 2초 폴링
- `main.py` — 라우터 등록 + 재시작 복구 훅 (`recover_on_startup`)
- `constants/robot_types.py` — `interactive_relay` work_mode 추가
- `TabletApp/MainActivity.kt` — URL을 `/api/dispatch/tablet/{id}`로 변경

운영 방식 변경 — 위치별 태블릿 호출 방식으로 재설계
- 관제 일괄 시작 폐기 → 각 작업 위치 태블릿에서 [호출] → 가용 로봇 자동 배정
- 가용 로봇 선정: `robot_status.battery_level` 내림차순 (충전 많은 순), 없으면 호출 거부
- 도착 후 같은 위치 태블릿에서 비어있는 다음 위치만 선택 가능
- 종료 버튼: 그 위치에 있는 로봇만 standby + 잭다운 + 충전소 복귀

신규 코드
- `dispatch_service.py` — `find_available_robot`, `get_session_at_poi`, `get_session_heading_to_poi`, `call_to_poi` 추가
- `schemas/dispatch.py` — `DispatchPOIStatusOut`, `DispatchCallResult` 추가
- `routers/dispatch.py` — POI 기준 API 5개 (`/poi/{id}/call`, `/poi/{id}/next`, `/poi/{id}/end`, `/poi/{id}/status`, `/tablet/poi/{id}`)
- `templates/dispatch_tablet.html` — POI 기준 UI로 전면 재작성 (empty: 호출 / calling: 대기 안내 / arrived: 다음 위치 그리드 + 종료)
- `TabletApp/MainActivity.kt` — 설정 항목 "로봇 ID" → "작업 위치 POI ID"

DB 마이그레이션 (rcs_basic_db → rcs_vesa_db)
- 18개 테이블 복제 (행 수 100% 일치)
- FK 22개 복원 (`CREATE TABLE LIKE`는 FK 미복사 — ALTER로 별도 추가)
- 백엔드 `DB_NAME=rcs_vesa_db`로 재시작 안내

VESA 태블릿 — 강제 제어 모달 추가 (5/29 추가됐던 항목 — 이후 위치 변경됨)
- `/api/dispatch/poi/{id}/status`에 `robot_ip` 필드 노출 → 프론트가 직접 기존 `/api/robots/remote/*` 호출
- 헤더 우상단 [강제 제어] 버튼 → 모달 (잭 업/다운, 이동 취소, 충전소 복귀, 대기장소 복귀)
- 자동 동작 중 멈춤/오작동 시 사용. 로봇이 이 위치에 있을 때만(`robot_ip` 있을 때) 버튼 활성
- 별도 백엔드 wrapper 라우터 만들 필요 없이 기존 `robots/remote` 라우터 재사용

프론트엔드 메인화면 정리
- `MonitoringClient.tsx`의 단일/배치 수동배차 탭(`DispatchTabs`) 제거
- 시작/배차는 위치별 태블릿이 담당 → 좌측 하단은 `ActiveJobsPanel`(진행 중 로봇 카드)만 유지
- `JackTestPanel`, `BatchDispatchPanel` 컴포넌트 파일은 보존 (재사용 가능성)
- 사이드바에서 "작업관리" 메뉴 제거 (자동 경로/스케줄은 VESA 미사용) — `/tasks` 페이지 파일은 보존
