# Python
__pycache__/
BackEnd/venv/

# Node
frontend/node_modules/
frontend/.next/

# Env
frontend/.env.local
.env


# prompts
frontend/spec/
frontend/CLAUDE.md
frontend/.codex/

# 업무보고
업무보고_*.md

# Android / TabletApp 빌드 산출물
TabletApp/.gradle/
TabletApp/app/build/
TabletApp/build/
TabletApp/local.properties

# DB 덤프 (로컬 보관용 — 운영 데이터라 커밋 금지)
_db/
*.sql

# 맵핑 산출물 (로봇에서 받은 로컬 파일. DB엔 경로만 저장됨)
BackEnd/static/maps/

# 자동 사이클 실행 로그 / 정지 시 스냅샷 (실행할 때마다 쌓임)
_logs/

nul
# 서버별 설정 (DB 비밀번호 포함) — config.env.example 만 커밋
BackEnd/config.env

# 프론트 정적 빌드 결과 (배포 시 생성)
BackEnd/web/
frontend/out/

# ── 2026-09-11 추가 — 인수인계 정리 ──
# 옛 스냅샷·백업본. 무엇이 현재 코드인지 헷갈리게 한다
_backup/
*.bak_*
*.bak

# APK 는 APK/ 에만 둔다 (tests/ 쪽은 중복 · 각 2.5MB)
tests/*.apk

# 파이썬 캐시
__pycache__/
*.pyc

# 진단 스크립트 산출물
_logs/run_*/
_logs/costmap_*/
BackEnd/_logs/

# 현장 이관용 패치 묶음 (필요할 때 다시 만든다)
RCS_LG_패치_*.zip

# 로그 수집 zip (6_로그모으기.bat 산출물)
로그수집_*.zip
venv_old/
.next_old_asus/
현장작업/사용폴더경로.txt
현장작업/차이_*.txt
현장작업/현장수정분_*.patch
_logs/bags/
